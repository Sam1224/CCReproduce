# CCReproduce

Daily paper reproductions for content ecosystem & creator governance.

- `YYYY-MM-DD/` contains reproductions implemented on that date.

> Note: Reproductions are *toy but runnable* implementations focusing on method logic and pipeline alignment (data/model/train/test). Some papers rely on proprietary datasets or large-scale infrastructure; those parts are simplified while keeping interfaces consistent.
